# -----------------------------------------------------------------------------
# Meu Álbum 2026 — variáveis de ambiente (raiz do projeto)
# -----------------------------------------------------------------------------
# Para o app mobile, veja AlbumApp/.env.example
# -----------------------------------------------------------------------------

# Supabase (para a CLI / migrations)
SUPABASE_PROJECT_ID=seu-projeto-ref
SUPABASE_DB_PASSWORD=senha-do-banco
SUPABASE_ACCESS_TOKEN=pat-gerado-no-dashboard

# Flask (dev)
FLASK_ENV=development
FLASK_APP=api.py
